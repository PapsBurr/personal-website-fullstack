name: Deploy Frontend and Backend with Terraform

on:
  push:
    branches: [main]
  workflow_dispatch:
    
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow not set up yet
        run: echo "This workflow is a placeholder for Terraform deployment."